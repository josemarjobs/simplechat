extends layout

block content
	script(type="text/template", id="messageTemplate")

	section.container
		header
			h1 Simple Chat App
		section
			section(id="messages-list", class="messages row")
				

			form(action="#", id="form")
				div.row
					div(class="col-sm-12")
						input(type="text", name="message", id="text")
					